ANTHROPIC_API_KEY=sk-ant-...
DATABASE_URL=postgresql://sparcos:sparcos@localhost:5433/sparcos_rag
VAULT_PATH=C:/Users/AlbertoDeCol/SecondBrain
EMBED_MODEL=bge-m3
EMBED_DIM=1024
OLLAMA_HOST=http://localhost:11434
ANSWER_MODEL=REPLACE_WITH_CURRENT_CLAUDE_MODEL_ID
TOP_K=10

# Index hygiene (P1.2). Leave unset to use the built-in DEFAULT_DENYLIST
# (excludes AGENTS.md/CLAUDE.md/COMMANDS.md/index.md, .trash/, .obsidian/,
# archives/templates/, archives/processed/). If you set this, it REPLACES the
# defaults entirely (no merge) — re-list every rule you still want. Comma-separated;
# trailing "/" = directory prefix, otherwise a filename.
# INDEX_DENYLIST=.trash/,.obsidian/,archives/templates/,archives/processed/,AGENTS.md,CLAUDE.md,COMMANDS.md,index.md
